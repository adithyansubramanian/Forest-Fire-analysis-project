fires <- read.csv("forestfires.csv")
#analyze which month had the most forest fires
#load dplyr and ggplot
#library("dplyr")
#library("ggplot2")
#group by month
#review_month <- fires %>%
#  group_by(month) %>%
#  summarize(total = n())


#ggplot(data = review_month) + 
#  aes(x = month, y = total) +
#  geom_bar(stat = "identity")

